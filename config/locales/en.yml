# Example of app specific locales ...
# (maybe put in super-mega.en.yml for example)

# en:
#   names:
#     slack_instance1: 'Work Slack'
#     slack_instance2: 'Fun Slack'
#     mediawiki_instance1: 'Random thoughts'
#     wordpress_instance: 'Super PR Stuff'
#   index:
#     header: 'Super-Mega Search'

en:
  searchers:
    index:
      placeholder: 'Search everything...'
    errors:
      errors: 'Errors have occurred'

  # Shamelessly stolen from ualbertalib/jupiter (thanks Murny!)
  views:
    pagination:
      first: "&laquo; <span class='d-none d-md-inline'>First</span>"
      last: "<span class='d-none d-md-inline'>Last</span> &raquo;"
      previous: "&lsaquo; <span class='d-none d-md-inline'>Prev</span>"
      next: "<span class='d-none d-md-inline'>Next</span> &rsaquo;"
      truncate: "..."
    otrs:
      queue: "Queue: %{queue}"
      age: "%{age} old"
      state: "%{state}"
      owner: "Owner: %{owner}"
      customer: "Customer: %{customer}"
    slack:
      redacted: 'Private message redacted'
      age: "%{age} old"
